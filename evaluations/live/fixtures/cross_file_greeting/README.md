# Greeting feature

Normalize display names by trimming the input, collapsing every whitespace run to one
space, and title-casing each word. `greeting()` must use the shared helper rather than
duplicating that logic.
